apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: blueprint-local

resources:
  - ../../base

labels:
  - pairs:
      environment: local

# Local development specific configurations
patches: []

# No replicas scaling for local development
replicas: []