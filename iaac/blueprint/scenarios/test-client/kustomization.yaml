apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - postgres-client.yaml
  - redis-client.yaml
  - curl-pod.yaml
  - debug-pod.yaml

commonLabels:
  scenario: test-client