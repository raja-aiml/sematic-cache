apiVersion: v1
kind: Service
metadata:
  name: semantic-cache
  namespace: app
  labels:
    app: semantic-cache
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: semantic-cache